# Optional release signing (gitignored). Used by app/build.gradle.kts.
# Copy to keystore.properties and fill in paths/passwords.
storeFile=/absolute/path/to/muzora-release.jks
storePassword=
keyAlias=muzora
keyPassword=
